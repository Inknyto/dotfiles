# ~/.nytoshell
# vim:ft=zsh

function mkcd() {
  mkdir -p "$1" && cd "$1"
}

function cpcd() {
  cp "$1" "$2" && cd "$2"
}

function mvcd() {
  mv "$1" "$2" && cd "$2"
}

function mkmv() {
  mkdir -p "$2" && mv "$1" "$2" && cd "$2"
}

function mkcp() {
  mkdir -p "$2" && cp "$1" "$2" && cd "$2"
}

